spring:
   application:
      name: spring-cloud-config-server
   cloud:
     config:
       server:
          git:
            uri: ${HOME}/IdeaProjects/HuisartsPortal/spring-cloud-config-server

server:
   port: 8888

