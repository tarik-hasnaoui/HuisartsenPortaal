spring:
   application:
      name: spring-cloud-config-server
   cloud:
     config:
       server:
          git:
            uri: file:///${user.home}/IdeaProjects/HuisartsPortal/git-locaconfig-repository

server:
   port: 8888

